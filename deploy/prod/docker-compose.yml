services:
  postgres:
    image: postgres:16.1
    container_name: prod_postgres
    restart: always
    volumes:
      - zm_db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=monddb
      - POSTGRES_USER=mond
      - POSTGRES_PASSWORD=${DB_PASSWORD}

  redis:
    image: redis:7-alpine
    container_name: prod_redis
    restart: always
    command: redis-server --appendonly yes
    volumes:
      - zm_redis:/data

  zm:
    hostname: zirkusmond_de
    image: ${DOCKER_USERNAME}/zirkusmond:latest
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - zm_media:/srv/data
      - zm_logs:/var/log/zirkusmond
    depends_on:
      - postgres
      - redis

  celery_worker:
    image: ${DOCKER_USERNAME}/zirkusmond:latest
    container_name: prod_celery_worker
    working_dir: /usr/src/backend
    command: celery -A config worker --loglevel=info
    env_file:
      - .env
    restart: unless-stopped
    depends_on:
      - postgres
      - redis

  celery_beat:
    image: ${DOCKER_USERNAME}/zirkusmond:latest
    container_name: prod_celery_beat
    working_dir: /usr/src/backend
    command: celery -A config beat --loglevel=info
    env_file:
      - .env
    restart: unless-stopped
    depends_on:
      - postgres
      - redis

networks:
  default:
    external: true
    name: ubuntu_default

volumes:
  zm_media:
    external: true
    name: ubuntu_zm_media
  zm_db:
    external: true
    name: ubuntu_zm_db
  zm_redis:
    external: true
    name: ubuntu_zm_redis
  zm_logs:
    external: true
    name: ubuntu_zm_logs
